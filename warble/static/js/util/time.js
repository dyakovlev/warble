/* Time Utils */

// returns current unix timestamp in seconds
export const ts = () => { return Math.round(Date.now()/1000) }

// returns current unix timestamp in milliseconds
export const mt = () => { return Date.now() }
